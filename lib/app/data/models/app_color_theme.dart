import 'package:flutter/material.dart';

class AppThemeColor {
  Color color;
  bool isActive;
  bool isDefaultAppThemeColor;
  AppThemeColor({
    required this.color,
    required this.isActive,
    required this.isDefaultAppThemeColor,
  });
}
